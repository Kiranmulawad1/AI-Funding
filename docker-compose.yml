version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PINECONE_API_KEY=${PINECONE_API_KEY:-}
      - PINECONE_ENV=${PINECONE_ENV:-}
      - PINECONE_INDEX_NAME=${PINECONE_INDEX_NAME:-funding-search}
      - PINECONE_NAMESPACE=${PINECONE_NAMESPACE:-openai-v3}
      - POSTGRES_URL=${POSTGRES_URL:-}
    volumes:
      # Mount data directory for persistence
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8501/_stcore/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  # Optional: PostgreSQL database
  # Uncomment if you want to run a local database
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     - POSTGRES_DB=funding_db
  #     - POSTGRES_USER=fundinguser
  #     - POSTGRES_PASSWORD=changeme
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

  # volumes:
  #   postgres_data:
